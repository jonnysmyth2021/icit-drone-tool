import { DescriptorProvider } from "../descriptor-provider"
export const militaryProvider = new DescriptorProvider({ id: "uk-military", country: "GB", authority: "Ministry of Defence / NATS AIS", capabilities: ["permanent", "infrastructure"], authoritative: true })
