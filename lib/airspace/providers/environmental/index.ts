import { DescriptorProvider } from "../descriptor-provider"
export const environmentalProvider = new DescriptorProvider({ id: "uk-environmental", country: "GB", authority: "UK statutory nature conservation bodies", capabilities: ["advisory"], authoritative: true, notes: "Environmental designations are advisory context unless a separate legal aviation restriction applies." })
