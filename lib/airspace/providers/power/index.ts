import { DescriptorProvider } from "../descriptor-provider"
export const powerProvider = new DescriptorProvider({ id: "uk-power", country: "GB", authority: "Configured energy asset custodians", capabilities: ["infrastructure"], authoritative: false })
