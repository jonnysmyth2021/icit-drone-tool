import { DescriptorProvider } from "../descriptor-provider"
export const policeProvider = new DescriptorProvider({ id: "uk-police", country: "GB", authority: "UK police data custodians", capabilities: ["temporary", "infrastructure"], authoritative: false })
