import { DescriptorProvider } from "../descriptor-provider"
export const utilitiesProvider = new DescriptorProvider({ id: "uk-utilities", country: "GB", authority: "Configured utility asset custodians", capabilities: ["infrastructure"], authoritative: false })
