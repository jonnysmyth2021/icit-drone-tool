import { DescriptorProvider } from "../descriptor-provider"
export const prisonsProvider = new DescriptorProvider({ id: "uk-prisons", country: "GB", authority: "Ministry of Justice / NATS AIS", capabilities: ["infrastructure", "permanent"], authoritative: true })
